# Copy to .env.local and fill in. Restart dev server after changes.

# Optional: when set, transactions/queue/execute/invoices use this backend (e.g. https://api.example.com)
# Leave unset to use same-origin Next.js API routes (local queue file).
# NEXT_PUBLIC_BACKEND_URL=

# Propose / initiate flow (proposeTransaction)
NEXT_PUBLIC_PIMLICO_API_KEY=
NEXT_PUBLIC_AGENT_ADDRESS=0x...
NEXT_PUBLIC_USDC_CONTRACT=0x...

# Server-side (API routes)
PIMLICO_API_KEY=
USDC_CONTRACT_ADDRESS=0x...
QUEUE_PATH=
STATE_PATH=
PATTERNS_PATH=
