# Owner EOA private key (0x-prefixed). Used as Safe owner/signer.
PRIVATE_KEY=0x...

# Pimlico API key for Sepolia bundler + paymaster.
# Get one at https://dashboard.pimlico.io
PIMLICO_API_KEY=your_pimlico_api_key

# Recipient address for the USDC transfer.
RECIPIENT_ADDRESS=0x...

# Amount of USDC to send (human-readable, e.g. 10.5). Default: 1
USDC_AMOUNT=1

# USDC_CONTRACT_ADDRESS=0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238

# --- Multisig (transfer-usdc-multisig.js) ---
# Owner keys you have (PRIVATE_KEY, PRIVATE_KEY2, ...). Need at least SAFE_THRESHOLD keys.
# For 2-of-3 with only 2 keys: set OWNER_ADDRESSES to all 3 addresses, PRIVATE_KEY + PRIVATE_KEY2 to the 2 keys.
# OWNER_ADDRESSES=0xaddr1,0xaddr2,0xaddr3
# SAFE_THRESHOLD=2
