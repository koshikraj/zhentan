# Path to the pending transactions queue JSON file (must be writable on this server) to be read by the OpenClaw agent
QUEUE_PATH=../zhentan-skills/pending-queue.json

# Path to the invoice queue JSON file (must be writable on this server) to be read by the OpenClaw agent
INVOICE_QUEUE_PATH=../zhentan-skills/invoice-queue.json


# Path to state.json for screening mode / decisions
STATE_PATH=../zhentan-skills/state.json

# Path to patterns.json for risk analysis (also determines skills directory for record-pattern.js) to be read by the OpenClaw agent
PATTERNS_PATH=../zhentan-skills/patterns.json

# Agent signer private key (for POST /execute co-signing)
# Generate a private key for your agent and use the signer address on client .env
AGENT_PRIVATE_KEY=

# Get your Pimlico bundler and paymaster API key from https://dashboard.pimlico.io
PIMLICO_API_KEY=

# Zerion API key (for portfolio / token balances on BNB Chain). Get one at https://zerion.io
ZERION_API_KEY=

# Server port
PORT=3001

